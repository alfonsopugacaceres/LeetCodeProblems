using System;
using System.Collections.Generic;
using System.Text;

namespace LeetCodeProblems.Problems.MinimumGroup1
{
    //1151. Minimum Swaps to Group All 1's Together
    //Given a binary array data, return the minimum number of swaps required to 
    //group all 1’s present in the array together in any place in the array.
    public class MinimumGroup1
    {
        public int MinSwaps(int[] data)
        {
            int ret = 0;
            

            for(int i = 0; i < data.Length; i++)
            {
                if()
            }
        }

        public int GroupAtIndex(int index, int[] data)
        {
            if(index == 0)
            {

            }
            else if(index == data.Length - 1)
            {

            }
            else
            {

            }
        }
    }
}
